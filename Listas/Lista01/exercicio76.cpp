// exercicio76.cpp
/*
Escreva um programa com uma função chamada somaImposto, que possui dois parâmetros:
taxaImposto, que representa a porcentagem de imposto que deve ser cobrado em relação ao valor
de um item, e custo, que representa o custo de um item antes da aplicação da taxa de imposto.
A função retorna o valor do custo do item, incluindo o valor do imposto. O programa deve
solicitar ao usuário a entrada do nome de um produto, do seu valor bruto e da taxa de imposto
que deve ser aplicada. Ele deve exibir uma mensagem informando o nome do produto, seu valor
sem os impostos, a taxa de imposto e o valor do produto com a aplicação da taxa de imposto.
*/

#include <iostream>

float somaImposto(float taxaImposto, float custo){


}

int main(){
    std::string nome;
    float valorProduto;
    float taxaImposto;

    std::cout << "\nPrograma para calculo de imposto.\n\n";

    std::cout << "* Informe o nome do produto: ";
    std::cin >> nome;
    

    return 0;
}